import os
os.system("cls")

"""
1. Fazer um procedimento chamado ‘preenche_lista(l)’ que preencha uma lista passada por parâmetro.
	preenche_lista(lista)
"""
def preenche_lista(lista):
    for i in range(4,0,1):
        lista = input("Preencha a lista: ")
        if lista == 0 :         
            else: 
                 break
